package com.nishant.service;

import org.springframework.stereotype.Service;

/**
 * This service will load all the runtime properties related to kafka
 * and otders required during runtime. The properties will be loaded from DB and kept in an
 * in-memory cache
 */
@Service
public class OrderConfigurationService {
}
